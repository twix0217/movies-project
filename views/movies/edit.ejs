<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Editing <%= movie.name %></title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="container">
        <h1>Edit <%= movie.name %></h1>
        <form 
            action="/users/<%= user._id %>/movies/<%= movie._id %>?_method=PUT"
            method="POST"
            class="edit-form"
        >
            <label for="name">Name:</label>
            <input 
                type="text" 
                name="name" 
                id="name" 
                value="<%= movie.name %>"
                required
            >
            <label for="genre">Genre:</label>
            <input 
                type="text" 
                name="genre" 
                id="genre" 
                value="<%= movie.genre %>"
                required
            >
            <label for="releaseDate">Release Date:</label>
            <input 
                type="date" 
                name="releaseDate" 
                id="releaseDate" 
                value="<%= movie.releaseDate ? movie.releaseDate.toISOString().substring(0, 10) : '' %>"
                required
            >
            <label for="imageURL">Image:</label>
            <input 
                type="text" 
                name="imageURL" 
                id="imageURL"
                value="<%= movie.imageURL %>"
                required
            >
            <label for="duration">Duration:</label>
            <input 
                type="number" 
                name="duration" 
                id="duration" 
                value="<%= movie.duration %>"
                required
            >
            <label for="director">Director:</label>
            <input 
                type="text" 
                name="director" 
                id="director" 
                value="<%= movie.director %>"
                required
            >
            <label for="description">Description:</label>
            <textarea 
                name="description" 
                id="description"><%= movie.description %></textarea>
            <label for="rating">Rating:</label>
            <input 
                type="number" 
                name="rating" 
                id="rating" 
                value="<%= movie.rating %>"
                required
            >
            <button type="submit">Update Movie</button>
        </form>
    </div>
    <footer>
        <p>&copy; 2024 Movies Database</p>
    </footer>
</body>
</html>
