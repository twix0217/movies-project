<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title><%= movie.name %></title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="container">
        <div class="movie-card-details">
            <h1><%= movie.name %></h1>
            <p><span>Genre:</span> <%= movie.genre %></p>
            <p><span>Release Date:</span> <%= movie.releaseDate %></p>
            <p><span>Director:</span> <%= movie.director %></p>
            <p><span>Description:</span> <%= movie.description %></p>
            <p><span>Rating:</span> <%= movie.rating %></p>
            <div class="buttons">
                <a href="/users/<%= user._id %>/movies/<%= movie._id %>/edit" class="btn">Edit</a>
                <form action="/users/<%= user._id %>/movies/<%= movie._id %>?_method=DELETE" method="POST" style="display: inline;">
                    <button type="submit" class="btn delete-btn">Delete</button>
                </form>
            </div>
        </div>
    </div>
    <footer>
        <p>&copy; 2024 Movie Database</p>
    </footer>
</body>
</html>
